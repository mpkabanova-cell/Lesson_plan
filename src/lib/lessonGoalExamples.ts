import type { LessonTypeId } from "@/lib/lessonTypes";

/**
 * Короткие примеры сильных формулировок цели — для подсказки учителю в интерфейсе
 * и как ориентир в системном промпте генерации цели.
 */
export const LESSON_GOAL_SHORT_EXAMPLES: Record<LessonTypeId, string> = {
  new_knowledge:
    "К концу урока ученики откроют новое правило (или способ), смогут объяснить его признаки и получат проверяемый учебный продукт (алгоритм, схема, решение и т.п.), опираясь на эталон.",
};

/** Развёрнутые примеры для системного промпта генерации цели. */
export const LESSON_GOAL_FULL_EXAMPLES_FOR_PROMPT = `
Пример сильной цели для урока открытия нового знания (ориентир по смыслу, не копируй дословно, если тема другая):

— «Ученики через пробное действие столкнутся с затруднением, откроют признак параллельности прямых через соответствующие углы; оформят эталон и применят признак в задаче на готовом чертеже с самопроверкой по эталону».
`.trim();
