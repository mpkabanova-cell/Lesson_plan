# OpenRouter API key (server only — never expose to the browser).
# Paste the key as-is (no "quotes"). On Render: Environment → add OPENROUTER_API_KEY.
OPENROUTER_API_KEY=

# Model id, e.g. openai/gpt-4o-mini
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional: shown in OpenRouter dashboard
OPENROUTER_HTTP_REFERER=
OPENROUTER_APP_TITLE=Конструктор плана урока

# Конструктор v3 (поэтапная генерация FGOS). false/0 — отключить, fallback на v2.
CONSTRUCTOR_V3_ENABLED=true
# Подпись сессии v3 (опционально; иначе используется OPENROUTER_API_KEY)
# CONSTRUCT_SESSION_SECRET=

# Max characters for lesson plan editor body (WYSIWYG)
NEXT_PUBLIC_PLAN_CONTENT_MAX_CHARS=100000

# Optional: trim methodology file (konstruktorUroka.md) when building the system prompt (server only)
# LESSON_METHODOLOGY_MAX_CHARS=120000

# Optional: trim subject-specific knowledge (e.g. informatika_7_9.md for Informatics grades 7–9) (server only)
# LESSON_SUBJECT_KNOWLEDGE_MAX_CHARS=120000

# Вкладка «Поиск материалов»: встроенный поиск Google (Programmable Search Element).
# Достаточно одного идентификатора (cx) из https://programmablesearchengine.google.com — передаётся с сервера:
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=
# Опционально дубликат с NEXT_PUBLIC_ (если нужен только клиентский бандл без пропа из page.tsx):
# NEXT_PUBLIC_GOOGLE_CUSTOM_SEARCH_ENGINE_ID=

# Яндекс Метрика и UX Feedback (клиент).
NEXT_PUBLIC_YANDEX_METRIKA_ID=108472990
NEXT_PUBLIC_UXFB_WIDGET_ID=nqn3hkbdzgrmumcqls7y36k1
# 0 или false — отключить отправку событий и загрузку счётчиков
NEXT_PUBLIC_ANALYTICS_ENABLED=1

# Опционально: Custom Search JSON API для POST /api/search-1sept (сервер).
# Во многих проектах Google требует привязанный Billing; без него часто 403.
# GOOGLE_CUSTOM_SEARCH_API_KEY=
